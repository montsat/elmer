//#include <BH1750FVI.h>
//BH1750FVI LightSensor;
//#include <SI7021.h>
//SI7021 sensor;

unsigned int lux;
double svit;


void setupCIDLA(){/********************************** SETUP CIDLA *****************************************/
   // Serial.println("==== START čidla ====");  
//      sensor.begin(21, 22); //PCF_02.write(LED_m1, LOW); stavPCF();   
// LightSensor.begin(); LightSensor.setMode(Continuously_High_Resolution_Mode); //LightSensor.Begin(Addr_LOW, Continuous_H);
}/*=======================================================================================================*/

void docidla() {
//teplota = sensor.getCelsiusHundredths(); teplota = teplota / 100; vlhkost = sensor.getHumidityPercent(); 
//lux = LightSensor.getAmbientLight(); svit=((1.00/((klux)/(lux)))*100.00); //lux = LightSensor.GetLux();
}/*====================================================================================*/
